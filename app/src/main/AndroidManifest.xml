<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onlineretailers">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.application.App"
        android:allowBackup="true"
        android:icon="@mipmap/timg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".displayview.login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".displayview.regist.RegistActivity" />
        <activity android:name=".displayview.shop.ShoppingMallActivity" />
        <activity android:name=".displayview.details.DetailsActivity"></activity>
    </application>

</manifest>